# Required project identification
sonar.projectKey=samyuktha215
sonar.projectName=recipesite
sonar.projectVersion=1.0

# Path to source code
sonar.sources=client/src

# Exclusions
sonar.exclusions=node_modules/**,dist/**

# Encoding
sonar.sourceEncoding=UTF-8

# Optional: language (JS/TS)
sonar.language=js

# Optional: coverage reports (if you have Jest or Vite coverage)
# sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Login token (CI should pass this via environment variable)
# sonar.login=${SONAR_TOKEN}
